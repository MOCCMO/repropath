export const projectPath = "/projects/fasttext-bag-of-tricks";

export const paths = {
  intake: "/",
  map: `${projectPath}/map`,
  checkpoint: `${projectPath}/checkpoints/run-minimal-target`,
  passport: `${projectPath}/passport`
} as const;
